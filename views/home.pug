extends frame.pug

block title
  | Login

block content
  h1 Welcome to Hive6!
  if isLogined
    a(href='/logout') #{username} Logout
  else
    a(href='/login') Login
    a(href='/register') Register
